services:
  mongo:
    image: mongo:7
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db

  siglip:
    build: ./services/siglip-embed
    ports:
      - '8001:8001'
    environment:
      - SIGLIP_MODEL=google/siglip-base-patch16-224

volumes:
  mongo_data:
